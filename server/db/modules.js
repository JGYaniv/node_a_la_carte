const { query } = require('./index')

async function getPopularModules() {
    //returns most popular module names & ids in descending order by total dls
}

// async function findMatchingModules(query) {
//     //returns the names & ids of all modules that match query string in desc order by total dls
// }

async function getModuleDetails(id) {
    //gets the module details from directory
}

async function postModule(moduleDetails){
    //posts a new module to the database
}

async function updateModule(moduleDetails){
    //updates the records for an existing module
}